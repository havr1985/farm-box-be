# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Category {
  id: String!
  name: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Farm {
  description: String
  id: ID!
  location: String!
  name: String!
  rating: Float
}

type Order {
  createdAt: DateTime!
  id: ID!
  items: [OrderItem!]!
  status: order_status!
  totalCents: Int!
  updatedAt: DateTime!
  user: User
  userId: ID!
}

type OrderItem {
  farmNameSnapshot: String!
  id: ID!
  priceCentsSnapshot: Int!
  product: Product
  productId: ID
  productNameSnapshot: String!
  quantity: Int!
}

input OrdersFilterInput {
  dateFrom: DateTime
  dateTo: DateTime
  status: order_status
}

input OrdersPaginationInput {
  cursor: String
  limit: Int = 20
}

type OrdersResponseType {
  nextCursor: String
  orders: [Order!]!
}

type Product {
  category: Category
  categoryId: String
  compareAtPriceCents: Int
  createdAt: DateTime!
  description: String
  expiresAt: DateTime
  farm: Farm!
  farmId: String!
  harvestDate: DateTime
  id: ID!
  imageUrl: String
  isActive: Boolean!
  isOrganic: Boolean!
  mainImageFileId: String
  name: String!
  priceCents: Int!
  sku: String!
  stock: Int!
  unit: productUnit!
}

input ProductsFilterInput {
  categoryId: String
  farmId: String
  isOrganic: Boolean

  """Max price in cents"""
  maxPrice: Float

  """Min price in cents"""
  minPrice: Float

  """Search by name"""
  search: String
}

input ProductsPaginationInput {
  limit: Int = 20
  page: Int = 1
}

type ProductsResponseType {
  hasNextPage: Boolean!
  items: [Product!]!
  page: Int!
  totalCount: Int!
  totalPages: Int!
}

type Query {
  adminUserOrders(filter: OrdersFilterInput, pagination: OrdersPaginationInput, userId: ID!): OrdersResponseType!
  health: String!
  order(id: ID!): Order
  product(id: ID!): Product!
  products(filter: ProductsFilterInput, pagination: ProductsPaginationInput): ProductsResponseType!
  userOrders(filter: OrdersFilterInput, pagination: OrdersPaginationInput): OrdersResponseType!
}

type User {
  email: String!
  id: ID!
  name: String!
  phone: String
}

"""Order status"""
enum order_status {
  CANCELED
  CONFIRMED
  DELIVERED
  PENDING
  PROCESSING
  SHIPPED
}

enum productUnit {
  G
  KG
  L
  ML
  PCS
}