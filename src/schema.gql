# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Order {
  createdAt: DateTime!
  id: ID!
  items: [OrderItem!]!
  status: order_status!
  totalCents: Int!
  updatedAt: DateTime!
  user: User
  userId: ID!
}

type OrderItem {
  farmNameSnapshot: String!
  id: ID!
  priceCentsSnapshot: Int!
  product: Product
  productId: ID
  productNameSnapshot: String!
  quantity: Int!
}

input OrdersFilterInput {
  dateFrom: DateTime
  dateTo: DateTime
  status: order_status
}

input OrdersPaginationInput {
  cursor: String
  limit: Int = 20
}

type OrdersResponseType {
  nextCursor: String
  orders: [Order!]!
}

type Product {
  createdAt: DateTime!
  id: ID!
  isActive: Boolean!
  name: String!
  priceCents: Int!
  stock: Int!
}

type Query {
  adminUserOrders(filter: OrdersFilterInput, pagination: OrdersPaginationInput, userId: ID!): OrdersResponseType!
  health: String!
  order(id: ID!): Order
  userOrders(filter: OrdersFilterInput, pagination: OrdersPaginationInput): OrdersResponseType!
}

type User {
  email: String!
  id: ID!
  name: String!
  phone: String
}

"""Order status"""
enum order_status {
  CANCELED
  CONFIRMED
  DELIVERED
  PENDING
  PROCESSING
  SHIPPED
}